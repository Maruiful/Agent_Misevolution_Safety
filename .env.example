# 项目根目录环境变量配置示例
# 用于 Docker Compose 部署

# MySQL 配置
MYSQL_ROOT_PASSWORD=root_password
MYSQL_USER=agent_user
MYSQL_PASSWORD=agent_password

# OpenAI API 配置
OPENAI_API_KEY=sk-xxx

# 注意：
# - 后端专用配置请查看：backend/.env.example
# - 前端专用配置请查看：frontend/.env.example
# - 各模块可独立运行，需要各自的 .env 文件
