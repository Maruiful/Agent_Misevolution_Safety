<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.agent.misevolution.repository.ExperimentMapper">

    <resultMap id="BaseResultMap" type="com.agent.misevolution.domain.Experiment">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="experiment_name" property="experimentName" jdbcType="VARCHAR"/>
        <result column="scenario" property="scenario" jdbcType="VARCHAR"/>
        <result column="config" property="config" jdbcType="VARCHAR"/>
        <result column="status" property="status" jdbcType="VARCHAR"/>
        <result column="total_episodes" property="totalEpisodes" jdbcType="INTEGER"/>
        <result column="current_episode" property="currentEpisode" jdbcType="INTEGER"/>
        <result column="start_time" property="startTime" jdbcType="TIMESTAMP"/>
        <result column="end_time" property="endTime" jdbcType="TIMESTAMP"/>
        <result column="created_at" property="createdAt" jdbcType="TIMESTAMP"/>
        <result column="updated_at" property="updatedAt" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, experiment_name, scenario, config, status, total_episodes, current_episode,
        start_time, end_time, created_at, updated_at
    </sql>

    <insert id="insert" parameterType="map" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO experiments (
            experiment_name, scenario, config, status, total_episodes, current_episode,
            start_time
        ) VALUES (
            #{experimentName}, #{scenario}, #{config}, #{status}, #{totalEpisodes},
            #{currentEpisode}, #{startTime}
        )
    </insert>

    <update id="updateStatus">
        UPDATE experiments
        SET status = #{status},
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <update id="updateCurrentEpisode">
        UPDATE experiments
        SET current_episode = #{currentEpisode},
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <update id="completeExperiment">
        UPDATE experiments
        SET status = 'completed',
            current_episode = #{currentEpisode},
            end_time = CURRENT_TIMESTAMP,
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM experiments
        WHERE id = #{id}
    </select>

    <select id="selectByName" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM experiments
        WHERE experiment_name = #{experimentName}
        LIMIT 1
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM experiments
        ORDER BY created_at DESC
    </select>

    <select id="selectByStatus" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM experiments
        WHERE status = #{status}
        ORDER BY created_at DESC
    </select>

</mapper>
